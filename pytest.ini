[pytest]
addopts = --alluredir=allure-results -vv -s
markers =
    unit: Unit tests for isolated functionality
    api: API tests for integration and endpoints
    validation: Tests for input validation
    length: Tests for string length constraints
    palindrome: Tests specific to palindrome generation
    non-palindrome: Tests specific to non-palindrome generation
testpaths = tests/

